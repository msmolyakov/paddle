{-# STDLIB_VERSION 3 #-}
{-# CONTENT_TYPE DAPP #-}
{-# SCRIPT_TYPE ACCOUNT #-}

@Callable(inv)
func checkMerkle(root: ByteVector, proof: ByteVector, value: ByteVector) = {
    WriteSet([
        DataEntry(inv.transactionId.toBase58String(), checkMerkleProof(root, proof, value))
    ])
}
